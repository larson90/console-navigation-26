/** Прототип: переход на главную страницу мегасервиса. */
export function navigateToMegaservice(megaserviceId: string) {
  // eslint-disable-next-line no-console -- навигация в прототипе
  console.log(`[nav] Переход на главную страницу мегасервиса: ${megaserviceId}`);
}
